name: secret
on:
  workflow_dispatch
env:
  ACTION_STEP_DEBUG: true
jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - name: "secret"
        run: |
          echo "secret value : ${{ secrets.MY_KEY }}"
          ${{ secrets.MY_KEY }}
