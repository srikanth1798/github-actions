name: secret
on:
  workflow_dispatch
env:
  ACTION_STEP_DEBUG: true
jobs:
  builds:
    run-on: ubuntu-latest
    steps:
      - name: "secret"
        run: |
          echo "secret value : ${{ secret.MY_KEY }}"
          ${{ secret.MY_KEY }}
